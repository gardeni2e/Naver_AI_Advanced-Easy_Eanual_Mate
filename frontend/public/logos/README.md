# Logo assets

Put brand logo files here if you want the UI to show the real logos.

Supported filenames:

- `gemini.webp`, `gemini.png`, or `gemini.svg`
- `youtube.webp`, `youtube.png`, or `youtube.svg`

The UI checks WebP first, then PNG, then SVG.
